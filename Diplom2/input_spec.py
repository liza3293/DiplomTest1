spec_text = """
Create a form section called Patient identity.
Use a two-column layout.
Add the following fields:
1. Patient ID — text field, required, placeholder: e.g. P-00123
2. External ID — text field, optional, placeholder: Hospital / registry ID
3. Gender — select field, required, options: Male, Female, Other
"""